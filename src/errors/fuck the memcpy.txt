memcpy是个蛋疼又不蛋疼的函数。。
我自以为了解它了，结果吃了屎。
我希望完成8bit数组到16bit数组的转换，我以为这样能行：
uint8  data[] = {0x00, 0x01, 0x00, 0x05};
uint16 id[2];
memcpy(id, (uint16*)data, 2);
结果发现id[0]正确，id[1]死都不对，为什么？


现在我终于知道原因了。。。因为memcpy的格式是：memcpy((void*), void*, uint);
于是。。。(uint16*) 就是个眼子了。。。

唉，用一个函数之前，一定要多了解它啊！！