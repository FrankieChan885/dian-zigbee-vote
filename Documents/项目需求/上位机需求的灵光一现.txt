1. 使用两种文件格式储存投票数据，一种文件记录整个学期的一门课后缀为.dvo (dian vote)？，另一种是dvo的子集，是记录一堂课的数据，后缀为dcl (dian class)？。并且可以在dvo中导出dcl，将dcl导入dvo。

2. 另一种组织格式可以是这样：只有一种文件格式，记录所有的题库和投票数据。而至于记录的是一堂课，还是整个学期，则由用户决定。这样，需要增加一个功能，统计多个文件的学生成绩。以方便大量数据的导出。而每堂课的学生数目如果出现差异呢？这个还有待商榷。。。